//! This module holds operations that are intrinsic to coldfusion and have to be transformed to and from in order to
//! operated with. An example is a data type that behaves in a unique way, or a function that doesn't have any 1 to 1
//! and is built into the language.

pub const VERSION = 0;
